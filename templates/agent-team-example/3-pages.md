You are a Page Builder. Build sub-pages for a SaaS analytics dashboard.

## Your files (create ONLY these — do not touch any other files):

### src/components/pages/AnalyticsPage.tsx
Traffic sources (donut chart), device breakdown (horizontal bars),
geographic distribution cards. Glass-card styling.

### src/components/pages/SettingsPage.tsx
API connection cards (4 integrations: connected/disconnected states).
Sync frequency selector. Notification toggles.

## Data import:
```typescript
import { analyticsData, settingsConfig } from "@/lib/mock-data";
```

## Tech stack: Next.js 15 + React 19 + recharts + Tailwind CSS 4

Create all files now: AnalyticsPage → SettingsPage.
