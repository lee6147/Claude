You are a Component Engineer. Build reusable UI components for a SaaS analytics dashboard.

## Your files (create ONLY these — do not touch any other files):

### src/components/KPICard.tsx
Glass-morphism card. Props: title, value, change (percentage), icon, color.
Animated count-up on mount. Sparkline mini-chart (SVG).

### src/components/AreaChart.tsx
Recharts AreaChart with gradient fill. 30-day data. Custom dark tooltip.

### src/components/BarChart.tsx
Recharts BarChart. Horizontal bars. Top 5 items ranking.

### src/components/DataTable.tsx
Sortable table with columns: name, value, change, status.
Status badge: green (up), red (down), gray (flat).

## Tech stack: Next.js 15 + React 19 + recharts + Tailwind CSS 4

Create all files now, in order: KPICard → AreaChart → BarChart → DataTable.
